basePath: /api/v1
info:
  contact: {}
paths:
  /example/fetchdocs:
    get:
      consumes:
      - application/json
      description: return docs in the order db
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: preturn docs in the order db
      tags:
      - example
swagger: "2.0"
